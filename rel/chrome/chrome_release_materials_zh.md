# Chrome Web Store 发布材料 — 猫盯鼠标 (Cat Eyeing Mouse) v1.0.0（中文版）

> 本文件包含发布到 Chrome Web Store 所需的全部流程说明与可复制的文字材料（中文版）。
> 英文版材料见 [chrome_release_materials_en.md](file:///d:/desktop/proj/9.cat_mouse/cat_eyeing_mouse/rel/chrome/chrome_release_materials_en.md)。

---

## 一、发布流程总览

| 步骤 | 操作 | 入口 |
|------|------|------|
| 1 | 注册开发者账号（一次性 5 美元注册费） | https://chrome.google.com/webstore/devconsole |
| 2 | 构建并打包扩展 ZIP（≤2MB） | 本地 `npm run build` + PowerShell 打包 |
| 3 | 开发者后台新建项目并上传 ZIP | Dashboard → 新建项 (New item) |
| 4 | 填写商店列表信息（名称/描述/分类/图形资产） | Dashboard → 商店列表 (Store listing) |
| 5 | 填写隐私惯例（单一用途/权限/数据声明） | Dashboard → 隐私惯例 (Privacy practices) |
| 6 | 设置分发范围（可见性/地区/价格） | Dashboard → 分发 (Distribution) |
| 7 | 提交审核（通常 1-3 个工作日，可能更长） | Dashboard → 提交以供审核 |
| 8 | 发布后维护（版本更新需重新提交审核） | Dashboard → 打包 (Package) |

---

## 二、各步骤需要填写的材料

### 步骤 1：注册开发者账号
- Google 账号登录
- 支付一次性注册费 **$5 USD**
- 开发者名称（将公开展示在商店页）
- 联系邮箱（公开显示，供用户反馈）
- 国家/地区

### 步骤 2：构建并打包 ZIP
```powershell
npm install
npm run build
Compress-Archive -Path manifest.json, _locales, src, res, dist -DestinationPath release/cat-eyeing-mouse-v1.0.0.zip -Force
```
- ZIP 内根目录必须直接可见 `manifest.json`（无嵌套目录）
- 必须包含：`manifest.json`、`_locales/`、`src/`、`res/`、`dist/content.js`
- 必须排除：`doc/`、`node_modules/`、`.trae/`、`readme.md`、`package*.json`
- 包体积 ≤ 2MB

### 步骤 3：上传 ZIP
- ZIP 上传后自动解析 manifest 中的 i18n 名称/描述/语言（en + zh_CN）

### 步骤 4：商店列表信息（每个语言一份）
| 字段 | 限制 | 本项目取值 |
|------|------|-----------|
| 扩展名称 | ≤75 字符 | 猫盯鼠标 |
| 简短描述 | ≤132 字符 | 见 [3.2](#32-简短描述商店列表字段) |
| 详细描述 | ≤16000 字符 | 见 [3.3](#33-详细描述商店列表字段) |
| 类别 | 单选 | 趣味 (Fun) |
| 语言 | — | 中文（简体）+ English |
| 图标 | 128x128 | `res/icons/icon128.png`（随包上传） |
| 截图 | 1280x800 或 640x400，1-5 张 | 见第四节 |
| 小宣传图 | 440x280，必需 | 见第四节 |
| 大宣传图 | 920x680 | 1 张，可选（用于推荐位） |

### 步骤 5：隐私惯例（Privacy practices）
- 单一用途说明（必填文本）→ 见 [3.5](#35-单一用途说明)
- 每个权限及主机权限的用途说明 → 见 [3.6](#36-权限用途说明)
- 数据使用声明（勾选）→ 见 [3.8](#38-数据使用声明勾选项)
- 远程代码声明 → 见 [3.7](#37-远程代码声明)
- 隐私政策 URL（建议填写）→ 见 [3.9](#39-隐私政策全文)

### 步骤 6：分发设置
- 可见性：公开 (Public)
- 地区：所有地区
- 价格：免费

### 步骤 7：提交审核
- 可附审核备注（建议填写，见 [3.10](#310-审核备注)）
- 审核结果通过注册邮箱通知

---

## 三、可复制的文字材料（中文版）

### 3.1 扩展名称
```
猫盯鼠标
```

### 3.2 简短描述（商店列表字段）
> ≤132 字符。manifest `_locales/zh_CN/messages.json` 中的 `app_description` 会自动填入，如需更丰富可在列表页覆盖。

```
一只可爱的小猫会追随你的鼠标并对移动做出反应。
```
（如需覆盖为更完整的版本，可用：）
```
在你的网页上养一只小猫：眼神与姿态实时追随鼠标，可拖拽、会休息、零隐私风险。
```

### 3.3 详细描述（商店列表字段）
```
猫盯鼠标——把一只软萌的小猫带到每一个网页里。

安装后，一只可爱的小猫会悬浮在页面右下角，用眼神和姿态实时追随你的鼠标移动，仿佛一只专注的猫咪正盯着自己的"猎物"。它不只是一件装饰，更是一个有生命感的桌面宠物：

【核心功能】
• 8 方向姿态跟踪：小猫根据鼠标方位实时切换 8 个朝向；鼠标移到它身上时，会进入专注的中心注视态
• 平滑过渡动画：基于 Canvas 交叉淡入淡出渲染，姿态切换丝滑无跳变
• 自由拖拽：按住小猫即可拖到页面任意位置，位置自动记忆，刷新页面不丢失
• 智能休息：鼠标静止 10 秒后，小猫会舒服地靠下休息；一动鼠标立刻精神抖擞
• 一键显隐：点击工具栏图标即可显示/隐藏小猫，还可开启视口边界约束，防止把猫咪拖丢
• 中英双语：自动适配浏览器语言，中文/英文界面开箱即用

【隐私与性能】
• 零数据收集：纯本地运行，无任何网络请求、无埋点、无数据上报
• 零配置：安装即用；Shadow DOM 完全隔离，不改动网页内容与样式
• 极致轻量：标签页隐藏时自动暂停渲染，鼠标静止时 CPU 占用接近 0%

【温馨提示】
出于浏览器安全限制，浏览器内置页面（如 chrome:// 设置页、Chrome 应用商店页面、PDF 查看器）中不会出现小猫，属正常现象。

如果你也想要一只永远盯着鼠标的小猫咪，现在就把它带回家吧！
```

### 3.4 类别与语言
- 类别：趣味 (Fun)
- 商店列表语言：中文（简体）、English（两份列表均需填写）

### 3.5 单一用途说明
```
在网页上显示一只跟随鼠标移动的猫咪动画桌面宠物（悬浮装饰），不提供其他功能。
```

### 3.6 权限用途说明
| 权限 | 填写内容 |
|------|----------|
| storage | 仅在用户本地 chrome.storage.local 保存猫咪悬浮位置与用户设置（显示/隐藏、边界约束），不上传任何数据。 |
| tabs | 将用户在工具栏弹窗中做出的显示/隐藏、边界约束开关操作，同步到已打开标签页中正在运行的猫咪悬浮层，使设置即时生效。不读取、不记录任何浏览数据。 |
| 主机权限 `<all_urls>` | 猫咪悬浮层需要能够渲染在用户访问的任意网页上。扩展仅注入一个 Shadow DOM 隔离的透明悬浮层，不读取、不修改页面内容。 |

### 3.7 远程代码声明
```
否，本扩展不使用、不执行任何远程代码。所有逻辑与资源均随扩展包分发。
```
（勾选：我不使用远程代码）

### 3.8 数据使用声明（勾选项）
- 收集的数据类别：**全部不勾选**（本扩展不收集任何用户数据）
- 勾选认证项：
  - [x] 我不会将用户数据出售或转让给第三方
  - [x] 我不会将用户数据用于与扩展单一用途无关的目的
  - [x] 我不会使用用户数据做信贷评估或借贷用途
- 本地数据处理：数据仅存储在用户本地 `chrome.storage.local`

### 3.9 隐私政策（全文）
> 建议托管到 GitHub 仓库或个人网站后，将 URL 填入"隐私政策 URL"字段。

```
猫盯鼠标（Cat Eyeing Mouse）隐私政策

最后更新：2026-08-16

我们非常重视你的隐私。本扩展不收集、不传输任何用户个人数据。

一、数据收集
本扩展不收集任何个人信息，包括但不限于浏览记录、输入内容、身份标识、位置信息。
本扩展不发起任何网络请求，无埋点、无统计、无数据上报。

二、本地存储
扩展仅在你的浏览器本地（chrome.storage.local）保存两类数据：
1. 猫咪的悬浮位置（键名 cem.position）
2. 你的显示/隐藏与边界约束设置（键名 cem.settings）
这些数据不会上传到任何服务器，卸载扩展后即被自动清除。

三、权限说明
- storage：用于在本地保存上述位置与设置；
- tabs：用于将你在弹窗中的开关操作同步到已打开的标签页；
- 所有网站访问（<all_urls>）：猫咪需要在你访问的任意网页上渲染悬浮层，
  扩展不读取、不修改任何页面内容。

四、第三方与远程代码
本扩展不接入任何第三方服务，不包含广告与统计 SDK，不执行任何远程代码。

五、政策变更
如未来功能变化涉及数据处理，我们将在更新说明中明确告知。

联系方式：[填写你的邮箱]
```

### 3.10 审核备注
```
本扩展为纯前端桌面宠物，安装后请打开任意普通网页（例如 https://example.com）验证：
1. 页面右下角出现猫咪，移动鼠标可见 8 个方向姿态实时切换；
2. 鼠标悬停到猫咪身上时显示中心注视态；按住猫咪可拖动到任意位置，刷新页面后位置保留；
3. 鼠标静止约 10 秒后猫咪进入休息态，移动鼠标立即唤醒；
4. 点击工具栏图标打开弹窗，可切换显示/隐藏与边界约束开关。

扩展无任何网络请求、无远程代码、不收集数据。
注意：浏览器内置页面（chrome://、Chrome 应用商店页、PDF 查看器）不会注入猫咪，属正常现象。
```

### 3.11 更新说明文案（版本发布时使用）
```
v1.0.0 首个正式版本：
• 8 方向姿态跟踪 + 悬停中心注视态
• Canvas 交叉淡入淡出平滑过渡
• 自由拖拽与位置记忆
• 鼠标静止 10 秒智能休息
• 弹窗一键显隐与边界约束
• 中英双语界面
```

---

## 四、图形资产要求（需另行制作）

| 资产 | 尺寸 | 数量/要求 |
|------|------|-----------|
| 扩展图标 | 128x128 | 随 ZIP 包内 manifest 提供（res/icons/icon128.png） |
| 截图 | 1280x800 或 640x400 | 1-5 张，PNG/JPG，展示猫咪跟随鼠标、拖拽、休息态、弹窗开关 |
| 小宣传图 | 440x280 | 1 张，必需 |
| 大宣传图 | 920x680 | 1 张，可选（用于推荐位） |
| 促销横幅 | 1400x560 | 1 张，可选 |

建议截图内容顺序：
1. 猫咪悬浮于页面右下角、目光跟随鼠标
2. 8 方向姿态切换（多方向对比图）
3. 按住猫咪拖拽
4. 静止 10 秒后的休息态
5. 工具栏弹窗（显隐 + 边界约束开关）

---

## 五、提交前检查清单

- [ ] `npm run build` 已执行，`dist/content.js` 为最新产物
- [ ] ZIP 根目录直接包含 `manifest.json`，体积 ≤ 2MB
- [ ] 商店列表已同时填写中文（简体）与 English 两个语言版本
- [ ] 简短描述 ≤132 字符；详细描述与实际功能一致
- [ ] 单一用途、三个权限说明已填写且与实际行为一致
- [ ] 数据使用声明全部勾选"认证"项，数据类别全部未勾选
- [ ] 隐私政策已托管并可公开访问，URL 已填入
- [ ] 图标、至少 1 张截图、440x280 小宣传图已上传
- [ ] 分发设置为：公开 / 所有地区 / 免费
- [ ] 审核备注已填写
